<form method="post" action="{{ path('app_reservations_deleteback', {'idRes': reservation.idRes}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation.idRes) }}">
    <button class="btn btn-secondary btn-sm btn-block">Delete</button>
</form>
