<form method="post" action="{{ path('app_map_art_delete', {'id': map_art.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ map_art.id) }}">
    <button class="btn">Delete</button>
</form>
