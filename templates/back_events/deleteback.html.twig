<form method="post" action="{{ path('app_events_deleteback', {'idEvent': event.idEvent}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ event.idEvent) }}">
    <button class="btn btn-secondary">Delete</button>
</form>
