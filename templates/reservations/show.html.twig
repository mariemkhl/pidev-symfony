{% extends 'base.html.twig' %}

{% block title %}Reservations{% endblock %}

{% block body %}
    <h1>Reservations</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdRes</th>
                <td>{{ reservation.idRes }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ reservation.name }}</td>
            </tr>
            <tr>
                <th>Datere</th>
                <td>{{ reservation.datere ? reservation.datere|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reservations_index') }}">back to list</a>

    <a href="{{ path('app_reservations_edit', {'idRes': reservation.idRes}) }}">edit</a>
    <a href="{{ path('app_reservations_pdf', {'idRes': reservation.idRes}) }}" class="btn btn-success" target="_blank">Generate PDF</a>

    {{ include('reservations/_delete_form.html.twig') }}
{% endblock %}