{% extends 'base.html.twig' %}

{% block title %}
    {% if reservation.idRes is not null %}
        Edit Reservations {{ reservation.idRes }}
    {% else %}
        Create New Reservations
    {% endif %}
{% endblock %}
	
 {% block body %}
   <div class="container">
     <h3 class="font-italic text-center">{% if reservation.idRes is not null %}Edit Reservations{% else %}Create New Reservations{% endif %}</h3>
{{ form_start(form) }}
        <div class="row">
         <div class="col-lg-8 offset-lg-2  text-center">
         <div class="error-text">
         <div class="form-group">
                            {{ form_label(form.name, 'Name') }}
                            {{ form_widget(form.name, { 'attr': {'class': 'form-control'} }) }}
                        </div>
                    

                 <div class="form-group">
                            {{ form_label(form.idUser, 'user') }}
                            {{ form_widget(form.idUser, { 'attr': {'class': 'form-control'} }) }}
                        </div>
                        
                        <div class="form-group">
                            {{ form_label(form.datere, 'Date') }}
                            {{ form_widget(form.datere, { 'attr': {'class': 'form-control'} }) }}
                        </div>
                        
                    
                     <div class="form-group text-center">
                            {{ form_label(form.idEvent, 'e ') }}
                            {{ form_widget(form.idEvent, { 'attr': {'class': 'form-control'} }) }}
                        </div>

                   
                     <div >
            <button type="submit" class="btn btn-primary">{{ button_label|default('Save') }}</button>
            <a href="{{ path('app_reservations_index') }}" class="btn btn-secondary">Cancel</a>
        </div>
        {{ form_end(form) }}


            {% if reservation.idRes is not null %}
           {{ include('reservations/_delete_form.html.twig') }}
                 {% endif %}
   
</div>
</div>
</div>
</div>
         <div class="col-lg-8 offset-lg-2  text-center">
      <a href="{{ path('app_reservations_index') }}">back to list</a>
      </div>

      

{% endblock %}
